let names = ['Nancy','Blessing','Jorge','Svetlana','Bob'];

names.filter(name => name.charAt(0)=== "B");

names.map( number => number.length);

names.reduce((total, namess) => total + namess.length,0)/namss.length;